import SwiftUI

struct Acknowledgements: View {
  var body: some View {
    Form {
  Section(
 header: Text("{{header}}")
 .font(.body)
 .padding(),
  footer: Text("{{footer}}")) {
{{#grouppkg}}
      Group {
{{#pkg}}
      NavigationLink("{{name}}", destination: ScrollView {
        VStack {
          Text(ack_{{functionFormatter(name)}}())
            .lineLimit(nil)
            .padding()
          Spacer()
        }
      })
{{/pkg}}
      }
{{/grouppkg}}
 }.navigationTitle("Acknowledgements")
    }
  }
  
  // MARK: - Licenses
  
  {{#grouppkg}}
{{#pkg}}
  func ack_{{functionFormatter(name)}}() -> String {
"""
{{license}}
"""
  }
{{/pkg}}
{{/grouppkg}}
}
